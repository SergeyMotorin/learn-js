
// Simple Array traversing

// Traverse an array directly from the array index

const words = ["Hello", "World"]

console.log(words[0])
console.log(words[1])


// Traverse an array using for loop

const fruits = [ "🍊", "🍐", "🍌", "🍏" ]
  
for (var i = 0; i < fruits.length; i++) {  
  console.log(fruits[i]);  
}